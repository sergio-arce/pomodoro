import React from 'react'

const Pause = ({ pause }) => {

    
    return <>
        <button
            onClick={ pause }
        >
            Pause
        </button>
    </>
}

export default Pause