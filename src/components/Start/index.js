import React from 'react'

const Start = ({ start }) => {

    
    return <>
        <button
            onClick={ start }
        >
            Start
        </button>
    </>
}

export default Start