import React from 'react'

const Reset = ({ reset }) => {
    
    return <>
        <button
            onClick={ reset }
        >
            Reset
        </button>
    </>
}

export default Reset